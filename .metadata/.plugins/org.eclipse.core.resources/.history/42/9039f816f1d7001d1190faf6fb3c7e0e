#pragma once

#include <vector>

using namespace std;

enum STATE{
	FREE,
	OCCUPIED
};


class Stream {
private:

	static vector<Stream> streams;
	uint32_t frequency;
	size_t size;
	void* ptr;

public:
	State state = OCCUPIED;

	Stream() {
		Stream::streams.push_back(this);
	}

	template<typename T>
	static void add_stream(uint16_t frequency, T* data) {
		for
	}

};
